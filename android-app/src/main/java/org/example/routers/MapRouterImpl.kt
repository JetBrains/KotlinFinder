package org.example.routers

import android.content.Context
import android.content.Intent
import com.icerockdev.jetfinder.feature.mainMap.MapRouter
//import com.icerockdev.jetfinder.feature.spotSearch.SpotSearchActivity

object MapRouterImpl : MapRouter {
    override fun routeToSpotSearch(context: Context) {
        //context.startActivity(Intent(context, SpotSearchActivity::class.java))
    }
}